{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "check:db": "node -e \"import('./src/db.js').then(async ({ checkDatabaseConnection, pool }) => { const result = await checkDatabaseConnection(); console.log(JSON.stringify(result, null, 2)); await pool.end(); }).catch((error) => { console.error(error.message); process.exit(1); });\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mysql2": "^3.22.5"
  }
}
